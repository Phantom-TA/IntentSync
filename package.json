{
  "name": "intentsync",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "cli": "pnpm --filter intentsync dev"
  },
  "devDependencies": {
    "@types/node": "^20.12.11",
    "esbuild": "^0.28.0",
    "tsx": "^4.11.0",
    "typescript": "^5.4.5"
  },
  "overrides": {
    "ioredis": "5.10.1"
  }
}
